<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>

    </style>

    <style name="AppActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="TransparentTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>

    </style>

    <style name="ToolbarStyle" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">#ffffff</item>
        <item name="android:textColorPrimary">#ffffff</item>
    </style>

    <style name="AppThemeDialog" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>

    </style>

    <style name="Theme.Transparent" parent="Theme.AppCompat">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- Style for buttons. -->
    <style name="ButtonDefault" parent="@android:style/Widget.Button">
        <item name="android:textSize">@dimen/font_default_1</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:layout_marginBottom">@dimen/margin_default_2</item>
        <item name="android:layout_marginTop">@dimen/margin_default_2</item>
        <item name="android:layout_marginLeft">@dimen/margin_default_4</item>
        <item name="android:layout_marginRight">@dimen/margin_default_4</item>
        <item name="android:layout_height">45dp</item>

    </style>

    <style name="ButtonDefaultSmall" parent="@android:style/Widget.Button">
        <item name="android:textSize">@dimen/font_default_1</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:layout_height">22dp</item>
        <item name="android:paddingLeft">10dp</item>

    </style>

    <style name="TextInputLayoutDefault" parent="AppTheme">
        <item name="colorAccent">@color/text_hint_focus
        </item>
    </style>


</resources>
